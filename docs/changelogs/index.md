# 更新日志

## 0.1.3 React Native 分支

- 发布包名调整为 `async-modal-render-native`。
- 移除 Web 静态挂载 API `asyncModalRender()`、`staticRender` 与 `react-dom` peer 依赖。
- 保留 Hook / Context / quiet / persistent / factory / destroy / mapper API。
- 测试切换为 React Native Testing Library。
